node_modules/
dist/
.playwright-mcp/
*.log
.DS_Store
Thumbs.db

.cache/
data/backups/
tmp/pdfs/

# Secrets. This repository is public — anything here would be published and
# indexed. .env.example is the committed template and holds no real values.
.env
.env.*
!.env.example

# Generated image derivatives (rebuilt from data/images)
data/thumbs/
data/medium/
data/originals/

# Generated export for the price-lookup tool (rebuild with: npm run export)
export/

# Temporary design working files and generated concept previews
.superdesign/tmp/
docs/prototypes/museum.html
docs/prototypes/engine.html

# Half-written print files. A locked PDF (one open in a viewer) leaves the
# temp HTML behind, and it was committed once.
print/.*.tmp.*
